name: Basic Pipeline

on:
  push:
    branches:
      - main

jobs:
  basic-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show files
        run: ls -la
